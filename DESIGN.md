# 语音转文字 - 设计思考

## 产品定位
一款极简的语音转文字工具——按住说话，松开即转文字，手机上显示特大号文字。核心场景：会议速记、灵感捕捉、长辈沟通。

## 气质与意象
- **意象锚点**：深夜书房里的一盏台灯，光线聚焦在桌面一张白纸上，笔尖刚刚离开纸面，墨迹未干
- **气质关键词**：安静、专注、纯粹、一字千金
- **用户情绪**：说话即被理解，文字厚重地浮现在眼前，无需任何多余操作

## 视觉策略
- **极致留白**：页面就是一张白纸，只有录音按钮和识别结果
- **文字即主体**：没有任何装饰性元素，识别出的文字就是页面上的唯一主角
- **动作即反馈**：按住 → 录音中（呼吸动画）→ 松开 → 文字浮现，一个动作闭环

## 配色方案
- **背景**：纯白（`bg-white`），模拟白纸质感
- **按钮**：柔和蓝灰色 (`bg-gray-100`)，非高强度视觉刺激
- **按钮激活态**：浅蓝色 (`bg-blue-50`)，轻声告知正在录音
- **文字**：深邃炭黑 (`text-gray-900`)，清晰但不刺眼
- **次级文字**：灰 (`text-gray-400`)，提示引导信息

## 字体排版
- **识别结果**：超大字号（`text-5xl` 起），一首诗能占满一屏的视觉重量
- **引导文案**：极小字号（`text-sm`），安静地待在角落里
- **行距**：宽松（`leading-relaxed`），让每个字都有呼吸空间

## 动效与交互
- **录音按钮**：按住时伴随柔和呼吸缩放的微反馈
- **文字出现**：无动画，直接浮现，干脆利落
- **运动性格**：克制、安静，不做无意义动效

## 设计禁忌
- ❌ 不要任何图标（包括话筒图标）—— 用"按住说话"文字直接表达
- ❌ 不要花哨的按钮样式 —— 普通圆形按钮即可
- ❌ 不要识别历史列表 —— 只显示最新一条结果
- ❌ 不要启动页引导 —— 打开即用